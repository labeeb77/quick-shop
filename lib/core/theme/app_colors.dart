import 'package:flutter/material.dart';

class AppColors {
  // Primary theme colors
  static const Color primary = Color(0xFF6200EE);
  static const Color primaryDark = Color(0xFF3700B3);
  static const Color secondary = Color(0xFF03DAC6);
  static const Color error = Color(0xFFB00020);
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFF000000);
  static const Color onError = Color(0xFFFFFFFF);
  static const Color onBackground = Color(0xFF000000);
  static const Color onSurface = Color(0xFF000000);

  // Auth screen specific colors
  static const Color authPrimary = Color(0xFF5D5FEF); // Purple button
  static const Color authInputBackground = Color(
    0xFFF5F5F5,
  ); // Light gray input fields
  static const Color authSecondaryBackground = Color(
    0xFFF5F5F5,
  ); // Gray button background
  static const Color authBorder = Color(
    0xFFE0E0E0,
  ); // Border color for outlined buttons

  // Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textWhite = Color(0xFFFFFFFF);

  // Common colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color transparent = Colors.transparent;
}
